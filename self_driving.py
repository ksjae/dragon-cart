import cv2
import pickle
import numpy as np
from picamera import PiCamera
from motor_control import Go, Turn
from time import time

Wheel_base = 700 #앞바퀴 중심 ~ 뒷바퀴 중심
Wheel_width = 670 #뒷바퀴 간격

def get_line(camera,fps):


def follow_line(speed,fps):
    camera = PiCamera()
    


